<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor do botão -->
    <solid android:color="#003C6E" />

    <!-- Cantos arredondados -->
    <corners android:radius="16dp" />

    <!-- Sombra leve -->
    <stroke
        android:width="2dp"
        android:color="#002A4C" />

    <!-- Margem interna -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>